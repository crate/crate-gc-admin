import { PropsWithChildren } from 'react';

export const Panel = ({ children }: PropsWithChildren) => {
  return children;
};

export const PanelGroup = ({ children }: PropsWithChildren) => {
  return children;
};

export const PanelResizeHandle = ({ children }: PropsWithChildren) => {
  return children;
};
